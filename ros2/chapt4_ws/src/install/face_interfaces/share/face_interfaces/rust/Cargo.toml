[package]
name = "face_interfaces"
version = "0.0.0"
edition = "2021"

[dependencies]
rosidl_runtime_rs = "0.6"
serde = { version = "1", optional = true, features = ["derive"] }
serde-big-array = { version = "0.5.1", optional = true }

# ROS Dependencies
std_msgs = "*"
sensor_msgs = "*"
builtin_interfaces = "*"
geometry_msgs = "*"

[features]
serde = ['dep:serde', 'dep:serde-big-array', 'rosidl_runtime_rs/serde', 'std_msgs/serde', 'sensor_msgs/serde', 'builtin_interfaces/serde', 'geometry_msgs/serde']

[package.metadata.ros-env]
include = true
